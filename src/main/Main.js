import React from 'react'
import { Switch, Route } from 'react-router-dom'
import PostList from '../components/postList'

const Main = () => (
	<Switch>
		{/* <Route exact path='/' component={TodoList} />
		<Route exact path='/post-list/:coach' component={PostList} />
		<Route exact path='/pokemon-list/:pokemon' component={PersonalPokemonList} /> */}
	</Switch>
)

export default Main;